[project]
name = "fastmcp-learning-demo"
version = "0.1.0"
description = "A two-day learning demo for FastMCP and MCP basics."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.2.4",
]
